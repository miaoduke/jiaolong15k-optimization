# 贡献指南（Contributing）

欢迎为本项目贡献。本项目是**特定设备（蛟龙15K / GM5BG0E）**的电源与风扇控制工具，规模小、单人主导，但完全开放。

## 最重要的铁律

> 🚫 **绝不把私有/敏感内容带入公开提交**。

以下内容**任何时候都禁止**进入 git 跟踪范围，提交前务必自查：

- `_private_不上传/` 整个目录（含脱敏前备份、真值凭据、机器个资、OEM 反编译产物）
- 官方「电竞控制台」反编译/抓包**原文**（`.pcap`、IL 转储、OEM 字符串 dump、UI 译文）
- 任何真实用户名、密码、Token、AES 密钥、SN、注册表快照

> 引用技术**结论**（寄存器地址、协议结构、纠错台账）是允许且鼓励的 —— 结论已固化为自写内容；引用**厂商源码/二进制/原文**是不允许的。

## 如何提交

1. **先 `git status` 预演**，确认改动清单里没有上述内容；跑一遍 `00_git_init_safety.ps1`。
2. 提交前对二进制/抓包等非文本文件保持警惕：能用文本固化的结论，就不要附原始文件。
3. 提交信息遵循「动词 + 对象 + 理由」风格（参考 `git log` 既有风格）。
4. 每个 PR / commit 专注一件事，方便回滚。

## 建议的改动分类

| 类型 | 说明 |
|---|---|
| `fix` | 修正寄存器结论、脚本 bug（建议附实机/回读证据） |
| `feat` | 新功能、新场景 |
| `docs` | 文档、README、CHANGELOG |
| `chore` | 清理、去重、规范化 |

## 行为准则

- 结论优先：任何"牌的寄存器 / 功耗值"都要有来源（实测、回读、纠错台账），不盲改文档。
- 涉及**写硬件**的代码改动，请在 README/注释中标注"已实测 / 待测"，避免他人误用。
- 有分歧时，先在对应 Issue 讨论，达成一致再改。