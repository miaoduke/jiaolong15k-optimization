# GitHub 公开部署 · 网页端逐步操作清单

> 适用：蛟龙15K优化方案仓库公开前，在 github.com 网页端完成的配置。
> 进度：**git 已完成本地初始化与安全首提交**（使用便携版 `D:\PortableGit`，git 2.55.0）。剩余第 2 步起的纯网页操作。
> 每完成一步打勾。

---

## ⚠️ 0) 先看：本机已就绪的部分（无需重复）

**已完成 ✓（2026-08-31，便携版 git 实测验证）**：

- [x] `git init`（主干 `main`）+ 中性署名（仓库级 `user.name/email`）
- [x] `_private_不上传/` 下 **1794 个私有文件逐文件 `git check-ignore` 全部被忽略**，无一泄漏
- [x] **安全首提交** `780ff86`：公开层 **326 文件** 全部跟踪，暂存区/提交中确认**无 `_private`**
- [x] 保密扫描（`git grep` 等价于 gitleaks）：**真实私钥/Token 形态 0 命中**；`AES_KEY_`/`REDACTED_PWD_SALT` 等命中均为 `<REDACTED_PWD_SALT>` 占位符，真值不在代码内
- [x] 公开层（00~04）无 `__pycache__` / `*.pyc` / `*.log` / `*.pyo` / `*.pcap` / `*.reg` 等
- [x] 已补齐 `.github/SECURITY.md`、`CONTRIBUTING.md`、`CHANGELOG.md`、`THIRDPARTY.md`、`.github/FUNDING.yml`（占位），并更新 README
- [x] 遗留：**gitleaks 未安装**（便携版不包含；可选继续，或用 GitHub 内置 Secret Scanning 兜底）
- [x] **逆向取证内容迁私**：13 个取证文件（抓包样本 / `dump_cred`/`get_cred2`/`get_key2`/`reflect_cm` / `DefaultTool反射` / `setup.ini` 系列）迁入 `_private_不上传/OEM反编译产物/01_协议_逆向成果_迁移/`（不入库）——提交 `60cd856`
- [x] **历史彻底清理**：用 `git filter-repo`（`pip install git-filter-repo`，Python 3.10 安装）将上述取证路径从**全部 commit 历史**中抹除；验证全历史 grep 残留为 0、对象经按文件归属核对无取证残留；工作区保持 **313 公开文件**，无 `_private` 跟踪。历史保留 3 条结构，hash 更新为 `12c3503 → 2fdef85 → 3dc7a95`

---

## 1) 首次提交（✅ 已完成，记录供追溯）

| 步骤 | 操作 | 校验 |
|------|------|------|
| 1.1 | git 2.55.0 便携版已置 `D:\PortableGit` | `git --version` ✓ |
| 1.2 | 运行 `00_git_init_safety.ps1`（UTF-8/BOM 问题致其语法报错）→ 改用**逐文件 `git check-ignore` 等价校验** | 1794 私有文件全部忽略 ✓ |
| 1.3 | `git init`（分支 `main`）+ 仓库级中性署名 `Jiaolong15K Optimization Contributors` | — |
| 1.4 | **安全 add**：逐个 `git add <公开目录>`（00~04 + .github + LICENSE/README 等） | 严禁 `git add -A` ✓ |
| 1.5 | `git status` / `git diff --cached --check` 人工核对 | 无 `_private` 无未知 ✓ |
| 1.6 | `git commit -m "chore: init 蛟龙15K优化方案公开层(脱敏/合规就绪)"` | `780ff86` ✓ |
| 1.7 | `git grep` 保密扫描（gitleaks 未安装） | 真实私钥/Token 0 命中 ✓ |

gitleaks 说明：便携版不含 gitleaks，当前可用网络环境。可选：
- `D:\PortableGit\...` 或 `go install github.com/gitleaks/gitleaks/v8@latest`（需有网）
- 或主仓库 push 后直接用 GitHub 内置 **Secret Scanning** 兜底（见步骤 4.4），跳过本地 gitleaks。本仓库已用 `git grep` 按 gitleaks 常见规则模式做过等价扫描，风险低。

---

## 2) 建仓（网页端）

- [ ] GitHub 登录：先开 **两步验证(2FA/TOTP)**（Settings → Password and authentication → Two-factor authentication）
- [ ] New repository（空仓，不勾选任何初始化文件，本地已有 README/LICENSE）
- [ ] 名称建议：`jiaolong15k-7425h-undervolt-console`（避开汉字/空格）
- [ ] 选中 **Private** 先推（见步骤 4.7 再改 Public），或直接按 SOP 公开（若你已确认风险）

---

## 3) 分支保护 + 社区文件

- [ ] **Actions 权限**：Settings → Actions → General → Workflow permissions 保持「Read repository contents」默认
- [ ] **分支保护**：Settings → Branches → Add rule → 主分支 → 勾选「Require pull request reviews before merging」（可选，单人仓库可视需关闭）
- [ ] 上传本机 SECURITY/CONTRIBUTING/FUNDING/THIRDPARTY/CHANGELOG（若未随 commit 推上去，可在仓库 Add file 逐份贴入，注意 UTF-8）

---

## 4) 安全与合规项（网页端）

| # | 项 | 位置 | 操作 |
|---|----|------|------|
| 4.1 | **默认分支** | Settings → General → Default branch | 确认为主线分支 |
| 4.2 | 禁用危险功能 | Settings → General → Features | 关 `Wikis`/`Projects`（可选，防泄私） |
| 4.3 | **依赖告警** | Settings → Security & analysis | 开 Dependabot alerts（本仓为文档+脚本，影响小） |
| 4.4 | **Secret Scanning** | Settings → Code security and analysis | 开「Secret scanning」+ **push protection** |
| 4.5 | **CodeQL** | Settings → Code security and analysis → Code scanning | Create 默认 CodeQL workflow（扫描 py/ps1） |
| 4.6 | 空扫描自检 | Actions 页 | 首次 CodeQL/secret 运行无 HIGH 即达标 |
| 4.7 | **转 Public** | Settings → General → Danger Zone | 确定后再转；转前再跑一遍 `00_git_init_safety.ps1` |

---

## 5) Sponsor 按钮（可选）

- [ ] 编辑 `.github/FUNDING.yml`，去掉你要用的渠道注释（github / patreon / custom 爱发电）
- [ ] 推送到主分支后，仓库右侧自动出现 Sponsor 按钮
- [ ] 若关闭捐赠：保持 FUNDING.yml 全注释即可，无按钮

---

## 6) 发布

- [ ] `README.md` 内 214→215 文件数、QR 占位、免责声明等已核对（README 已同步）
- [ ] 建议发布一个 `v0.1.0` Tag：Releases → Create a new release → 填 tag → 自动附 changelog

---

## 收尾自检（对本机）

- [ ] 本机 git init 后 `_private_不上传` 仍隔离
- [ ] 未 commit 任何 `C:\Users\d6150` 路径 / 真值 / 反编译原文
- [ ] 若安装了 gitleaks 记得后续复用；未安装则不污染环境

**遗留唯一阻塞项**：本机网络下载不到 git/gitleaks（`0x80072efd`）。其余（离线安全门验证、FUNDING/README/社区文件）均已完成，可在有网环境一次性收尾。