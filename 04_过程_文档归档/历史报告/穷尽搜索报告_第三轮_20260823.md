# 穷尽搜索补充报告 + 风扇UI回归修复（2026-08-23 第三轮）

## 一、风扇独立控制/曲线消失 — 根因与修复 ✅

### 根因（第二轮补丁引起的回归）
| 参数 | 补丁写入值 | 原始值 | 后果 |
|------|-----------|--------|------|
| `FAN.FanSwitchSpeed` | 2200/2500 RPM | **300** | 超出UI滑条有效范围 → 控件隐藏 |
| `FAN.SkipSafetyAbnormalProtection` | 1 | 0 | 跳过安全保护 → UI 隐藏相关控制 |
| `M3T1.FanControlRespective` | true | false | 与服务端状态不一致 |
| `M3T1.Activated/CpuTemp_DefaultMaxLevel` | true/15 | false/11 | 表结构异常 |

### 已执行修复（管理员提权完成）
- M3T1.json 完整还原原始
- Mode3/Mode4 的 FAN 段恢复：Speed=300 / Enabled=0 / SkipSafety=0
- MainOption FanBoostEnable=0
- **保留** Mode3/Mode4 其余性能字段（PL/TGP/OC——本就被硬件钳制，无副作用）
- **重启控制台 UI 即可看到风扇独立控制和曲线恢复**

> 教训：风扇参数有严格范围校验，第三方改写极易触发 UI 保护性隐藏。以后动风扇只用 MQTT 正规通道。

## 二、穷尽搜索 — 新发现的遗漏文件（本轮全部补齐）

### A. DefaultTool\（★ 最大遗漏，EC 操作核心）
| 文件 | 说明 |
|------|------|
| **DefaultTool.exe v3.0.0.11 (1MB)** | .NET 程序集，已反射枚举 336 类型 |
| processcontrol.reg | 进程控制注册表（Edge 亲和性示例） |
| UEFI_Firmware.dll v1.0.0.1 | 固件读写 |

**DefaultTool 反射成果**：
- `OemService.Exec(Int32, String[], Int32, Byte[])` + `OemSvcHook` — OEM 命令分发器
- `OemCustomerCmdList.AP = ' /GGYY7878'` ← **DefaultTool 隐藏命令行参数！**
- `LM_Manager/LM_EC_RGB/LM_ITE_RGB` — 完整灯光 API：
  - `SetEffectALL(Mode,Effect,light,speed,Direction,Color,NV_SAVE,...)`
  - ITE HID 协议：`HID_Set_Effect_Type_08H / Brightness_09H / TimeOut_1AH`
- `RGBKB_Effect` 全 28 灯效枚举（Single=0...Dawn=27，BatteryPercent=25）
- `RGBKB_Mode`：Lighting=1 / Welcome=2 / Night=3
- **工厂重置工具**：`FanTable.ClearUserFanTables()`、`SmartApcTable.ClearSmartApcTableReg()/ClearUserPofiles()`
- 内嵌依赖：M2Mqtt、NAudio(音乐律动)、SharpDX.RawInput(键鼠钩子)
- MQTT 客户端身份 = `OcTool/OcToolUser/OcToolPwd_[REDACTED]`

### B. 其他新发现组件
| 位置 | 内容 |
|------|------|
| AirplaneDriver\ | devcon.exe + vhidmini 虚拟HID驱动（飞行模式键） |
| CreateShortcut\ | 3个快捷方式生成器变体 |
| GamingCenter\ | ControlCenterU.exe / GamingCenterU.exe 启动器 |
| logo\Language\zh-cn.json | 托盘文案（含"究极进化"等完整词条） |
| logo\TrayIcon\ | 办公/均衡/狂暴三态托盘图标 |
| OSDTpDetect.exe | 触控板OSD悬浮层（27类型，走MQTT，Fody织入） |

### C. GCUService 内嵌依赖全清单（33资源）
关键项：**IntelOverclockingSDK**（Intel专用）、**M2Mqtt.net**（服务也有MQTT客户端）、**System.Management.Automation**（内嵌PowerShell宿主！）、Dapper+EF+SQLite（本地数据库）、NAudio、colourful(RGB库)、RegistryUtils

### D. 系统级
- 服务：GCUBridge（唯一）；驱动：UWACPIDriver 运行中；无计划任务
- UWP 包 ControlCenter3 v5.17.49.19 安装位置确认
- 已安装包 vs 解包差异仅 7 个运行时生成文件（Q&A.html、合并PRI），无实质遗漏
- C:\Program Files\Plugin 不存在（插件系统未安装）

## 三、结论

**至此该软件的所有相关文件已 100% 盘点完毕**：
```
已反编译/反射: GamingCenter3_Cross(AOT,字符串分析) · GCUService · GCUBridge
              · SystrayComponent · DefaultTool ★ · OSDTpDetect
已文档化协议:  MQTT(13688,凭证+Topic+Action) · EC寄存器147地址 · RGB灯效28种
未深挖项:      UWACPIDriver.sys(原生C++,接口已被上层覆盖) · AirplaneDriver(标准vhid)
价值评估:      无进一步高价值遗漏 —— DefaultTool 是最后一个金矿且已开采
```

## 四、遗留行动项
1. 用户重启控制台 UI 确认风扇功能恢复
2. 如需再次调整风扇：只走 MQTT 通道（`Fan/Control` + 合法范围内的 JSON 指令），不再直接改 JSON 文件
